# Frontend & UI Skills

This repo's catalog does not currently ship a frontend build skill.
`frontend-reference-hunt` (sources visual references from Dribbble/Mobbin/etc.) is the one
frontend-adjacent skill in `curated-skills.txt`.

`ui-expert`, `frontend-design`, `tailwind-design-system`, and `ai-slop-audit` were retired
2026-07-16 and folded into `repaint`, the author's canonical frontend build skill in the
full personal harness (not currently in this repo's curated set). See `docs/composites.md`
for the archived-skills table.

`ui-audit`, `design-build`, `shadcn`, `nextjs-patterns`, and `vercel-patterns` (previously
documented here) are not part of this repo's curated skill set.

**Last updated:** 2026-08-02
