# RTK

If `rtk` is installed and working, prefer it for noisy CLI output reduction.
If it is missing or outdated, fall back to normal commands without drama.
Do not make RTK a single point of failure for normal development.
