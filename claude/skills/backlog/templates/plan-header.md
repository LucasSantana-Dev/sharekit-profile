---
repo: {{OWNER}}/{{REPO}}
generated_by: /backlog
generated_at: {{ISO_TIMESTAMP}}
backlog_run_id: {{RUN_ID}}
sources:
  audit_deep: {{AUDIT_DEEP_MEMORY_PATH}}
  ecosystem_health: {{ECOSYSTEM_HEALTH_STATUS}}
  repo_state_snapshot: {{SNAPSHOT_JSON_PATH}}
---

# Backlog {{DATE}}

Generated by `/backlog`. Each task below becomes one GitHub issue via
`plan-to-issues`. Phase numbers map to scheduling tiers, not implementation
order:

- **Phase 1 — Critical & High:** address this week
- **Phase 2 — Medium:** address this month
- **Phase 3 — Low:** address when convenient (or close as `not_planned`)

Re-running `/backlog` on this repo will dedup against issues with the
`backlog-skill` label, so closing an issue without resolving the underlying
finding will resurface it on the next run.
