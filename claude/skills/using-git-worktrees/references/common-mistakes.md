# Common Mistakes

- creating a worktree without checking whether the current checkout is dirty,
- inventing a directory convention instead of reusing the repository's existing one,
- skipping the minimal setup step and assuming the new worktree is ready,
- forgetting to report the new path and branch back to the user.
