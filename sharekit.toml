[profile]
name = "lucassantana"
version = "0.3.0"
description = "Lucas Santana's Claude Code workflow — operator CLAUDE.md + 60 portable skills + a file-based memory-structure template (the methodology, not the content)"
