[profile]
name = "lucassantana"
version = "0.4.0"
description = "Lucas Santana's Claude Code workflow — operator CLAUDE.md + 64 portable skills + a zero-setup file-based memory system (recall/sync/knowledge-loop/prune work out of the box; optional git vault + semantic retriever)"
