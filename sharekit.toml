[profile]
name = "lucassantana"
version = "0.6.0"
description = "Lucas Santana's Claude Code workflow — operator CLAUDE.md + 65 portable skills + a zero-setup file-based memory system (recall/sync/prune; advanced memory-eval gate) + Megabrain (one-vault-for-all-projects: memory + graphs + RAG)"
