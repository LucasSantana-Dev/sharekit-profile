[profile]
name = "lucassantana"
version = "0.5.0"
description = "Lucas Santana's Claude Code workflow — operator CLAUDE.md + 64 portable skills + a zero-setup file-based memory system + Megabrain (the one-vault-for-all-projects methodology: memory + graphs + RAG)"
