[profile]
name = "lucassantana"
version = "0.8.0"
description = "Lucas Santana's Claude Code workflow — operator CLAUDE.md + 66 portable skills + a zero-setup file-based memory system (recall/sync/prune; advanced memory-eval gate) + Megabrain (one-vault-for-all-projects: memory + graphs + RAG; bootstrap-project stands up a new project's memory + central graph in one ritual)"
