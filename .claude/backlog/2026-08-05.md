# Backlog 2026-08-05 — sharekit-profile hygiene sweep

Source: `/backlog` run, discover.sh + audit-deep (test-health, config-drift-detect,
hook-effectiveness, security-audit, mcp-audit, plugin-audit, socket-audit) +
manifest integrity check + manual repo inspection.
Prior backlog: `.claude/backlog/2026-08-04.md` — 8/10 findings shipped (#122-126, #129-131).
Issues `#127` (bats coverage) and `#128` (eval fixtures) still open, not re-proposed.
Open issues pre-run: 2 (#127, #128). ROI = (severity × urgency × value) / effort, value 1-5.

Notable: no package.json in this repo (test-health/socket-audit N/A for npm surface).
Manifest integrity, secret scan, .env handling, branch protection all clean — no findings.

## Ranked findings

| # | Finding | Severity | Effort | ROI | Evidence |
|---|---------|----------|--------|-----|----------|
| 1 | Two hook trees (`hooks/` 50 tracked, `claude/hooks/` 71 tracked) with no doc reconciling which is canonical or how they relate | medium | S | 3.0 | `git ls-files hooks/ \| grep -E '\.sh$' \| wc -l` = 50; `git ls-files claude/hooks/ \| grep -E '\.sh$' \| wc -l` = 71; AGENTS.md only describes `hooks/` |
| 2 | Untracked worktree `.claude/worktrees/agent-a8161036304728c30/` (4.1MB, dated 2026-08-04) not covered by `.gitignore` | low | XS | 2.0 | `git ls-files .claude/worktrees/` = 0 tracked; not in `.gitignore`; `du -sh` = 4.1M |
| 3 | AGENTS.md "Hook count: 49" (line 79) stale again 1 day after being fixed in #125 — actual is 50 in `hooks/` | low | XS | 2.0 | `git ls-files hooks/ \| grep -E '\.sh$' \| wc -l` = 50 vs AGENTS.md:79 |

## Themes

- **Repo hygiene** (all 3): tech-debt/docs, low blast radius, no functional risk

## Non-issue action

- Comment on existing #127 with corrected evidence: its cited "18 bats files vs 49 hooks"
  understates the real gap — actual `claude/hooks/` has 71 hooks (71 vs 18 bats, not 49 vs 18).

## Propose gate

Approved: user instruction "All" (2026-08-05), covering all 3 findings + the #127 comment update.

## Run summary

- Created: 3 issues (#146, #147, #148), labels `backlog` + type/severity/effort.
- Comment left on existing: #127 (corrected bats-coverage evidence).
- Board: `Active Backlog` (project #4) — all 3 added. Note: a second, stale
  `sharekit-profile Backlog` board (project #7) also exists from the 2026-08-04
  run; the two boards have diverged from the skill's single-board design and
  should be reconciled (merge or retire #7) in a future pass.
- Specs: skipped — no feature findings, all tech-debt/docs.
- Execution order: all 3 are XS/S effort, no particular sequencing constraint.
